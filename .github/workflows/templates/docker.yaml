name: Build Docker Images

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      tag_suffix:
        required: false
        type: string
        default: ""

jobs:
  build-images:
    name: Build Docker images
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: backend
            context: backend
            file: backend/Dockerfile
          - name: worker
            context: backend
            file: backend/Dockerfile.worker
          - name: frontend
            context: frontend
            file: frontend/Dockerfile
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.file }}
          push: true
          tags: |
            ghcr.io/${{ github.repository | lower }}/labomatics-${{ matrix.name }}:v${{ inputs.version }}${{ inputs.tag_suffix }}
            ghcr.io/${{ github.repository | lower }}/labomatics-${{ matrix.name }}:latest${{ inputs.tag_suffix }}
