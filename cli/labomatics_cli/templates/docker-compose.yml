x-common-env: &common-env
  WAN_INTERFACE: {{ WAN_IFACE }}
  DOMAIN: {{ DOMAIN }}

services:
  postgres:
    image: postgres:16-alpine
    container_name: labomatics-postgres
    environment:
      POSTGRES_PASSWORD: {{ PG_ROOT_PASSWORD }}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-databases.sh:/docker-entrypoint-initdb.d/init-databases.sh
    networks:
      - labomatics
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  keycloak:
    image: quay.io/keycloak/keycloak:24.0.5
    container_name: labomatics-keycloak
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: {{ KEYCLOAK_DB_PASSWORD }}
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: {{ KEYCLOAK_ADMIN_PASSWORD }}
      KC_PROXY_HEADERS: xforwarded
      KC_HOSTNAME: keycloak.{{ DOMAIN }}
      KC_HOSTNAME_STRICT: false
      KC_HTTP_ENABLED: true
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - labomatics
      - proxy
    restart: unless-stopped
    command: start-dev
    labels:
      - traefik.enable=true
      # HTTP
      - "traefik.http.routers.keycloak-http.rule=Host(`keycloak.{{ DOMAIN }}`)"
      - "traefik.http.routers.keycloak-http.entrypoints=web"
      - "traefik.http.routers.keycloak-http.service=keycloak"
      - "traefik.http.routers.keycloak-http.middlewares=https-redirect@file"
      # HTTPS
      - traefik.http.routers.keycloak-https.rule=Host(`keycloak.{{ DOMAIN }}`)
      - traefik.http.routers.keycloak-https.entrypoints=websecure
      - traefik.http.routers.keycloak-https.tls=true
      - traefik.http.routers.keycloak-https.service=keycloak
      - traefik.http.services.keycloak.loadbalancer.server.port=8080

  ldap:
    image: bitnami/openldap:2.6
    container_name: labomatics-ldap
    user: "0:0"
    environment:
      LDAP_ROOT: "{{ LDAP_BASE_DN }}"
      LDAP_ADMIN_USERNAME: admin
      LDAP_ADMIN_PASSWORD: "{{ LDAP_ADMIN_PASSWORD }}"
      LDAP_ENABLE_TLS: "yes"
      LDAP_TLS_CERT_FILE: /opt/bitnami/openldap/certs/ldap.{{ DOMAIN }}.crt
      LDAP_TLS_KEY_FILE: /opt/bitnami/openldap/certs/ldap.{{ DOMAIN }}.key
      LDAP_TLS_CA_FILE: /opt/bitnami/openldap/certs/ca.crt
      LDAP_CUSTOM_LDIF_DIR: /ldifs
    volumes:
      - ldap_data:/bitnami/openldap
      - ./ldap/bootstrap.ldif:/ldifs/01-bootstrap.ldif:ro
      - ./certs:/opt/bitnami/openldap/certs:ro
    ports:
      - "389:1389"
      - "636:1636"
    networks:
      - labomatics
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "ldapsearch", "-x", "-H", "ldap://localhost:1389", "-b", "{{ LDAP_BASE_DN }}", "-s", "base"]
      interval: 10s
      timeout: 5s
      retries: 5

  radius:
    image: freeradius/freeradius-server:3.2.5
    container_name: labomatics-radius
    volumes:
      - ./radius/clients.conf:/etc/raddb/clients.conf:ro
      - ./radius/mods-enabled/ldap:/etc/raddb/mods-enabled/ldap:ro
      - ./radius/sites-enabled/default:/etc/raddb/sites-enabled/default:ro
      - ./certs/ca.crt:/etc/raddb/certs/ca.crt:ro
    ports:
      - "1812:1812/udp"
      - "1813:1813/udp"
    networks:
      - labomatics
    depends_on:
      ldap:
        condition: service_healthy
    restart: unless-stopped

  traefik:
    image: traefik:v3.7.9
    container_name: labomatics-traefik
    command:
      - --configFile=/etc/traefik/traefik.yml
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik.yml:/etc/traefik/traefik.yml:ro
      - ./dynamic:/etc/traefik/dynamic:ro
      - ./certs:/etc/traefik/certs:ro
      - traefik_logs:/var/log/traefik
    networks:
      - proxy
    restart: unless-stopped
    labels:
      - traefik.enable=true
      # - traefik.http.routers.traefik-http.rule=Host(`traefik.{{ DOMAIN }}`)
      # - traefik.http.routers.traefik-http.entrypoints=web
      # - traefik.http.routers.traefik-http.middlewares=https-redirect@file
      - traefik.http.routers.traefik-https.rule=Host(`traefik.{{ DOMAIN }}`)
      - traefik.http.routers.traefik-https.entrypoints=websecure
      - traefik.http.routers.traefik-https.tls=true
      - traefik.http.routers.traefik-https.service=api@internal
      #- traefik.http.routers.traefik-https.middlewares=admin-chain@file

networks:
  proxy:
    driver: bridge
  labomatics:
    driver: bridge


volumes:
  postgres_data:
    driver: local
  ldap_data:
    driver: local
  traefik_acme:
    driver: local
  traefik_logs:
    driver: local
